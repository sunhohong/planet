<%= show_for @test do |s| %>
  <%= s.attribute :title %>
  <%= s.attribute :description %>
<% end %>

<div class="actions">
  <%= link_to 'Edit', edit_test_path(@test), :class=>"btn" %>
  <%= link_to 'Back', tests_path, :class=>"btn" %>
</div>
